# nas_vm: TrueNAS appliance — package installs get reverted on updates.
- name: Install and configure Tailscale on all hosts
  hosts: all:!nas
  become: true
  roles:
    - role: tailscale
  tags:
    - tailscale
