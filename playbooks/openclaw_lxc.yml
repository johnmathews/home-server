- name: OpenClaw LXC
  hosts: openclaw_lxc
  gather_facts: true
  become: true

  roles:
    - role: openclaw_lxc
      tags: openclaw
    - role: shell_environment
      tags: shell
    # tailscale installed manually via web UI (apt-key not available on Debian 13)
