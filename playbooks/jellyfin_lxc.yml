# playbooks/jellyfin_lxc.yml

- name: Jellyfin LXC
  hosts: jellyfin_lxc
  gather_facts: true
  become: true

  roles:
    - role: nfs_client
      tags: nfs
    - role: share_drive_probe
      tags: shares
    - role: jellyfin_lxc
      tags: jelly
    - role: shell_environment
      tags: shell

  # nfs vars in /host_vars
