# playbooks/proxmox_node.yml

- name: Proxmox node
  hosts: proxmox
  become: true
  roles:
    # its safer to mount NFS shares directly into priviliged LXCs 
    # - role: nfs_client
    #   tags: nfs
    # - role: share_drive_probe 
    #   tags: shares
    - role: pve
      tags: pve
    - role: shell_environment
      tags: shell
    - role: tailscale
      tags: tailscale
