these scripts are not deployed or managed using ansible.
they are either:
  - one-off helper scripts (e.g. collect-tailscale-ips.sh, immich-empty-album.sh)
  - scripts that run locally and interact with the server (yt-dlp-on-media-vm.sh)
