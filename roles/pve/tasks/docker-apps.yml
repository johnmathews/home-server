# tasks/docker-jellyfin.yml
# /srv/jellyfin/config

---


- name: Download Docker GPG key (Bookworm-compatible)
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/debian/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: '0644'

- name: Add Docker apt repository
  ansible.builtin.deb822_repository:
    name: docker
    types: deb
    architectures: amd64
    uris: https://download.docker.com/linux/debian
    suites: "{{ ansible_distribution_release }}"
    components: stable
    signed_by: /etc/apt/keyrings/docker.asc
    state: present

- name: Remove the superseded Docker .list repository file
  ansible.builtin.file:
    path: /etc/apt/sources.list.d/docker.list
    state: absent

- name: Install Docker
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
    state: latest
    # deb822_repository does not refresh the apt cache; apt_repository used to.
    update_cache: true

- name: Enable and start Docker service
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Install Docker Compose plugin
  ansible.builtin.apt:
    name: docker-compose-plugin
    state: present

- name: Ensure Docker Compose directory exists
  ansible.builtin.file:
    path: /srv/apps
    state: directory


- name: Create base app directories for containers
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
    owner: root
    group: root
  loop:
    - /srv/apps/alloy

- name: Upload config files
  ansible.builtin.copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: "{{ puid }}"
    group: "{{ guid }}"
    mode: '0644'
  loop:
    - alloy:
      src: files/config.alloy
      dest: /srv/apps/alloy/config.alloy
  become: true
  tags: alloy
  notify:
    - Restart alloy

- name: Deploy docker-compose.yml
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: /srv/apps/docker-compose.yml
    owner: root
    group: root
    mode: '0644'
  tags: portainer

- name: Deploy .env file template
  ansible.builtin.template:
    src: templates/.env.j2
    dest: /srv/apps/.env
    owner: root
    group: root
    mode: '0644'
  tags: portainer

- name: Start apps using Docker Compose
  community.docker.docker_compose_v2:
    project_src: /srv/apps
    pull: never
  tags: portainer
