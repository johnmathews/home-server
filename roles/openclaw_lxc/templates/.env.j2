# vim: ft=bash

# OpenClaw environment variables

TZ={{ TZ }}

# Gateway token (set after running: docker compose run --rm openclaw-cli onboard --no-install-daemon)
OPENCLAW_GATEWAY_TOKEN={{ vault_openclaw_gateway_token | default('') }}

# Claude Max subscription auth (session cookies from claude.ai)
CLAUDE_AI_SESSION_KEY={{ vault_claude_ai_session_key | default('') }}
CLAUDE_WEB_SESSION_KEY={{ vault_claude_web_session_key | default('') }}
CLAUDE_WEB_COOKIE={{ vault_claude_web_cookie | default('') }}

# Paths
OPENCLAW_CONFIG_DIR=/srv/apps/openclaw/config
OPENCLAW_WORKSPACE_DIR=/srv/apps/openclaw/workspace

# Network
OPENCLAW_GATEWAY_PORT=18789
OPENCLAW_BRIDGE_PORT=18790
OPENCLAW_GATEWAY_BIND=lan

# Image
OPENCLAW_IMAGE=openclaw:local
