# vim: ft=systemd

[Unit]
Description=Unpause media containers at {{ docker_quiet_hours_end }}

[Timer]
OnCalendar={{ docker_quiet_hours_end }} Europe/Amsterdam
Timezone=Europe/Amsterdam
Persistent=false

[Install]
WantedBy=timers.target
