# vim: ft=systemd

[Unit]
Description=Pause media containers at {{ docker_quiet_hours_start }} (Europe/Amsterdam)

[Timer]
OnCalendar={{ docker_quiet_hours_start }} Europe/Amsterdam
Timezone=Europe/Amsterdam
Persistent=false

[Install]
WantedBy=timers.target
