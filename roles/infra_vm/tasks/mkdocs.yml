# infra_vm/tasks/mkdocs.yml

---

- name: Ensure mkdocs docs directory exists
  ansible.builtin.file:
    path: /srv/infra/mkdocs/docs
    state: directory
    owner: "{{ docker_user }}"
    group: "{{ docker_user }}"
    mode: '0755'
  notify: Restart mkdocs

- name: Template mkdocs.yml
  ansible.builtin.template:
    src: mkdocs.yml.j2
    dest: /srv/infra/mkdocs/mkdocs.yml
    owner: "{{ docker_user }}"
    group: "{{ docker_user }}"
    mode: '0644'
  notify: Restart mkdocs

- name: Copy project markdown docs into mkdocs docs dir
  ansible.builtin.copy:
    src: ../documentation/
    dest: /srv/infra/mkdocs/docs/
    owner: "{{ docker_user }}"
    group: "{{ docker_user }}"
    mode: '0644'
  notify: Restart mkdocs
