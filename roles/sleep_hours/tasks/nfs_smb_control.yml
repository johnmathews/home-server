---
# TrueNAS NFS/SMB share control via API
# This task file is a placeholder for future implementation
# It will be expanded when NFS/SMB control is added to the sleep-hours workflow

# Example future implementation:
# - name: Disable NFS/SMB shares during sleep hours (TrueNAS API)
#   block:
#     - name: Call TrueNAS API to disable shares
#       uri:
#         url: "{{ truenas_api_url }}/api/v2.0/smb/share"
#         method: PATCH
#         auth_digest: "{{ truenas_api_auth }}"
#         body_format: json
#         body: { "enabled": false }
#       when: sleep_hours_disable_nfs_smb | default(false)
#       tags: ['sleep-hours-nfs']
#
#     - name: Call TrueNAS API to enable shares
#       uri:
#         url: "{{ truenas_api_url }}/api/v2.0/smb/share"
#         method: PATCH
#         auth_digest: "{{ truenas_api_auth }}"
#         body_format: json
#         body: { "enabled": true }
#       when: sleep_hours_enable_nfs_smb | default(false)
#       tags: ['sleep-hours-nfs']

- name: NFS/SMB control not yet implemented
  ansible.builtin.debug:
    msg: "NFS/SMB share control will be implemented in a future update"
  when: sleep_hours_disable_nfs_smb or sleep_hours_enable_nfs_smb
