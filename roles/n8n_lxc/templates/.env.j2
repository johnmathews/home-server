# PostgreSQL password
POSTGRES_PASSWORD={{ vault_n8n_postgres_password }}

# n8n encryption key (critical - must persist)
# Generate with: openssl rand -hex 16
N8N_ENCRYPTION_KEY={{ vault_n8n_encryption_key }}
